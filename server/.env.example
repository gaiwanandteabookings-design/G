# Скопируйте этот файл в .env и заполните реальными значениями перед запуском в проде.

PORT=3000

# Ставьте в production, когда сайт реально задеплоен (включает редирект http->https).
NODE_ENV=development

# Ставьте true только если сайт работает за реверс-прокси/хостингом (Render, Railway,
# nginx и т.д.), который сам выставляет заголовки X-Forwarded-*. Иначе оставьте пустым.
TRUST_PROXY=

# Логин/пароль для входа в /admin.html (заявки, инвойсы). ОБЯЗАТЕЛЬНО смените на что-то
# надёжное перед реальным запуском — короткий числовой пароль легко подобрать перебором.
ADMIN_USERNAME=profix305
ADMIN_PASSWORD=1234

# Google Analytics 4 Measurement ID (вида G-XXXXXXX). Оставьте пустым, чтобы аналитика
# на сайт не подключалась вообще.
GA_MEASUREMENT_ID=

# --- Email (уведомления о заявках + отправка инвойсов) ---
# Письма отправляются через SendGrid API (HTTPS), а НЕ через обычный SMTP — на Render
# бесплатный план блокирует исходящий SMTP (порты 25/465/587) полностью, соединение
# просто зависает навсегда. SendGrid шлёт письма как обычный HTTPS-запрос, это не блокируется.
# Если SENDGRID_API_KEY не задан, письма просто не отправляются, заявки/инвойсы всё равно
# сохраняются в БД.
#
# ВАЖНО про папку "Спам": если FROM_EMAIL — это адрес на gmail.com (или любом чужом
# домене), письма почти гарантированно будут падать в спам или отклоняться — Gmail не
# разрешает SendGrid отправлять письма "от имени" @gmail.com (нет SPF/DKIM подписи для
# чужого домена). Раз у тебя уже есть свой домен profix305.com — используй Domain
# Authentication вместо Single Sender Verification, это решает проблему полностью.
#
# Как настроить (один раз, ~10 минут):
# 1. Зарегистрируйся на sendgrid.com (бесплатный план — 100 писем/день).
# 2. Settings -> Sender Authentication -> Authenticate Your Domain -> введи profix305.com.
# 3. SendGrid покажет 3 CNAME-записи — добавь их в DNS домена на Namecheap (Advanced DNS),
#    подожди подтверждения (обычно несколько минут, иногда до пары часов).
# 4. Settings -> API Keys -> Create API Key -> Full Access -> скопируй ключ (виден один раз).
SENDGRID_API_KEY=
# Куда присылать уведомления о новой брони (можно оставить на gmail — это же просто ПОЛУЧАТЕЛЬ,
# проблема со спамом касается только адреса-ОТПРАВИТЕЛЯ ниже):
NOTIFY_EMAIL=profix305@gmail.com
# От чьего имени отправлять — должен быть на домене, который подтверждён через Domain
# Authentication (шаг 2-3 выше). Сам адрес не обязан существовать как реальный почтовый
# ящик — profix305.com уже твой домен, использовать можно любое имя перед @:
FROM_EMAIL="ProFix305 <notifications@profix305.com>"

# --- База данных (Turso — бесплатная, переживает передеплой) ---
# Без этих двух переменных БД живёт в локальном файле server/data/bookings.db — это ок
# для локальной разработки, но на Render бесплатном тарифе этот файл стирается при
# каждом передеплое (все заявки/инвойсы пропадают). Как настроить — см. README.md
# ("Making bookings/invoices survive redeploys").
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# --- Автоматический запрос отзыва после оплаты инвойса ---
# Как только отмечаешь инвойс "Paid" в админке, клиенту автоматически уходит письмо
# с просьбой оставить отзыв и прямой ссылкой на форму отзыва Google. Пусто = функция
# выключена (просто не будет искать ссылку и не отправит письмо).
# Как получить ссылку: после того как одобрят Google Business Profile, зайди в профиль
# на google.com/business → "Get more reviews" — там будет короткая ссылка вида
# https://g.page/r/XXXXXXXX/review — скопируй её сюда целиком.
GOOGLE_REVIEW_URL=

# --- SMS-бот для заявок (Twilio) ---
# Клиент пишет на твой бизнес-номер — бот сам по смс задаёт те же вопросы что и форма
# на сайте, и в конце создаёт настоящую заявку в базе (видна в /admin.html как обычная).
# Без этих переменных функция просто не будет работать (проверка подписи Twilio не
# пройдёт без TWILIO_AUTH_TOKEN — это специально, защита от поддельных запросов).
#
# Как настроить (см. README.md, раздел "SMS-бот"):
# 1. Зарегистрируйся на twilio.com, купи номер с SMS (или подключи текущий бизнес-номер).
# 2. Console -> Account -> API keys & tokens -> скопируй Account SID и Auth Token.
# 3. В настройках купленного номера -> "A MESSAGE COMES IN" -> Webhook ->
#    https://www.profix305.com/api/sms/inbound (HTTP POST) — ссылка должна совпадать
#    с этой ТОЧНО, иначе проверка подписи не пройдёт.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
